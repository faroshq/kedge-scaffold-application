{
  "name": "kedge-scaffold-application-web",
  "private": true,
  "type": "module",
  "description": "Frontend tier of the kedge application scaffold. Served at / on the app's single public URL; calls the backend same-origin at /api/*.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.mjs"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
